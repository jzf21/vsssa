{
  "name": "vsssa",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rvf dist & cp -r src/html dist & node-sass src/scss/style.scss dist/assets/style.css",
    "serve": "live-server --open=./dist"
  },
  "devDependencies": {
    "node-sass": "^7.0.1"
  },
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
